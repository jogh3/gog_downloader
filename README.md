# GOG downloader

a way of downloading GOG game executables on linux


## Arguments
```  
    --set-download-location DIRECTORY_LOCATION sets the location where the mods will download to
    --help -h displays this message
```


### **Ensure curl and JQ is installed on your machine**
